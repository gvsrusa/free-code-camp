function bouncer(arr) {
    // Don't show a false ID to this bouncer.
    return arr.filter(el=>el);
  }
  
  bouncer([7, "ate", "", false, 9]);



// function bouncer(arr) {
//     // Don't show a false ID to this bouncer.
//     return arr.filter(el=>{
//       if(!el===false||null||0||""||undefined||NaN)
//       return el;
//     });
//   }
  
//   bouncer([7, "ate", "", false, 9]);

// Note: return won't return falsy values.